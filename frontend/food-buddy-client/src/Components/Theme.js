import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#269660',
      contrastText: '#fff',
    },
    background: {
      default: "#ede9d2"
    }
  },
});

export default theme;
